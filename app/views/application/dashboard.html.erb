<h3 align="center">Welcome Admin(s)</h3>
<div class="container" id="allfiles" style="overflow:scroll; height:400px;">
<div class="row">
<div class="col s6">
<h4 align="center">A list of all files</h4>
  <table class="bordered highlight">
    <thead>
      <tr>
        <th>User</th>
        <th>File Name</th>
        <th>File Type</th>
      </tr>
    </thead>
        <% @uploads.each do |upload|%>
    <tbody>    
      <tr>
        <td> <%= upload.user.username%></td>
        <td> <%=link_to "#{upload.name}", upload_path(upload)%></td>
        <td> <%= upload.content_type%></td>
      </tr>
        <%end%>
    <tbody>
  </table>
  </div>
  </div>
</div>
      
      
